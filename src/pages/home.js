import React from "react";

import { FaqsContainer, FooterContainer, HeaderContainer, JumbotronContainer, FeatureContainer } from "../containers";

const Home = () => (
  <>
    <HeaderContainer>
      <FeatureContainer />
    </HeaderContainer>
    <JumbotronContainer />
    <FaqsContainer />
    <FooterContainer />
  </>
);

export default Home;
